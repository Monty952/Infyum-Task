<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_rounded_card"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="24dp"
        android:text="Choose App"
        android:textColor="@color/wa_text_primary"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- WhatsApp Button -->
    <Button
        android:id="@+id/btnOptionWhatsApp"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/bg_choose_app_button"
        android:text="@string/btn_choose_whatsapp"
        android:textAllCaps="false"
        android:textColor="@color/wa_text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:backgroundTint="@null" />

    <!-- WhatsApp Business Button -->
    <Button
        android:id="@+id/btnOptionWaBusiness"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginBottom="12dp"
        android:background="@drawable/bg_choose_app_button"
        android:text="@string/btn_choose_wa_business"
        android:textAllCaps="false"
        android:textColor="@color/wa_text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:backgroundTint="@null" />

    <!-- File Manager Button -->
    <Button
        android:id="@+id/btnOptionFileManager"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@drawable/bg_choose_app_button"
        android:text="@string/btn_choose_file_manager"
        android:textAllCaps="false"
        android:textColor="@color/wa_text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        app:backgroundTint="@null" />

</LinearLayout>
